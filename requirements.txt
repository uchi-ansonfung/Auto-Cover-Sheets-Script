# Runtime dependencies (kept in sync with pyproject.toml)
# Prefer: pip install -e ".[dev]"
# Optional: pip install -e ".[ocr]" / ".[optimize]" / ".[full]"
reportlab>=4.0,<5
pypdf>=4.0,<6
customtkinter>=5.2,<6
